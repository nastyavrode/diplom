┌─────────────────────────────┐
│           Клиент            │
│        (React Native)       │
│                             │
│  - Экраны приложения        │
│  - Локальное хранилище      │
│    (AsyncStorage)           │
│  - Статический контент      │
│    (уроки, челленджи)       │
└─────────────┬───────────────┘
        │ HTTP/HTTPS
        ▼
┌─────────────────────────────┐
│           Сервер            │
│        (Node.js/Express)    │
│                             │
│  - API маршруты             │
│    (/api/auth, /teacher)    │
│  - Логика аутентификации    │
│    (JWT, bcrypt)            │
│  - Проверка ролей           │
│    (учитель/студент)        │
└─────────────┬───────────────┘
        │ REST API
        ▼
┌─────────────────────────────┐
│        База данных          │
│          (MongoDB)          │
│                             │
│  - Модель User               │
│    (name, email, password,   │
│     role, classId, progress) │
│  - Хранение прогресса        │
│    учеников                  │
│  - Хранение классов          │
└─────────────────────────────┘